Workshop 3.1 Custom Module คือการแยกไฟล์โค้ดออกมาเป็นส่วนๆ เพื่อให้เรียกใช้งานซ้ำได้ โดยใช้ module.exports เพื่อส่งออกฟังก์ชัน และใช้ require() เพื่อนำเข้าไปใช้งานในไฟล์หลัก 
Workshop 3.2 Callback & Promis เป็นวิธีจัดการงานที่ต้องใช้เวลา Asynchronous โดย Callback คือการส่งฟังก์ชันเข้าไปเป็นพารามิเตอร์เพื่อให้เรียกใช้เมื่อจบงาน ส่วน Promise คือออบเจ็กต์ที่เป็นตัวแทนของผลลัพธ์ในอนาคต จัดการผลลัพธ์ผ่าน .then() และ .catch() 
Workshop 3.3 Async/Await เป็นวิธีการเขียนโค้ดที่ทำงานแบบ Asynchronous ให้ดูเหมือนโค้ดปกติที่รันตามลำดับ Synchronous โดยใช้ await เพื่อรอผลลัพธ์จาก Promise และใช้ try/catch เพื่อดักจับข้อผิดพลาด 
Workshop 3.4 Parallel Promises คือการสั่งงานหลายอย่างให้เริ่มทำพร้อมกันแบบขนาน โดย Promise.any() จะเอาผลลัพธ์จากงานที่สำเร็จเป็นตัวแรกสุด ส่วน Promise.allSettled() จะรอจนทุกงานจบลงแล้วรายงานสถานะทั้งหมดไม่ว่าจะสำเร็จหรือล้มเหลว